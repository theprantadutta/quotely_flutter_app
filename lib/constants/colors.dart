import 'package:flutter/material.dart';

// Re-export the theme colors for backward compatibility
export '../theme/colors/app_colors.dart';

/// Legacy helper color - now uses warm accent
/// Kept for backward compatibility with existing code
const Color kHelperColor = Color(0xFFE8A84C); // Golden amber accent

/// Quick access to primary warm colors
const Color kWarmAmber = Color(0xFFC17D3D);
const Color kWarmBrown = Color(0xFF8B5E34);
const Color kWarmCream = Color(0xFFFFF8F0);
const Color kWarmDark = Color(0xFF3D2914);
